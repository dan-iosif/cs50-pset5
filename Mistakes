This code assigned the same address to all pointers in hashtable[i]

 node* hashtable[10];
    for(int i=0; i<10;i++)
    {
        node* new_node=malloc(sizeof(node));
        if (new_node==NULL) return false;
        
        new_node -> next = NULL;
        strcpy(new_node->word,"zzzz");
        hashtable[i]=new_node;
    }
    free(new_node);
